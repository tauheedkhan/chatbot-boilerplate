export default function discourseHandler(r) {

  // Ths is the place where you can intersept the response for the watson server and do the manupluation for the UI, for example setting the context.
return r;
}
